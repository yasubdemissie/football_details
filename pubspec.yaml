name: football_my_app
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  intl: ^0.18.0
  animated_bottom_navigation_bar: ^1.3.3
  fl_chart: 0.69.2

flutter:
  uses-material-design: true

  assets:
    - assets/
